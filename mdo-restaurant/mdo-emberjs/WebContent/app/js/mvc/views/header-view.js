'use strict';

/**
 * This file will contain Mdo Header view.
 * This HeaderView view is useless because emberjs can generate it for you by wrapping "script metamorph start/end" around the template.
 * In order to avoid script generation, we define the HeaderView view then emberjs wraps a div around the template.
 */

Mdo.HeaderView = Ember.View.extend({
	templateName: "header",
});

